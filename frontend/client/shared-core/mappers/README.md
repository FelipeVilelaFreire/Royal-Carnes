# Client Mappers

Mappers do fluxo do cliente.

Papel:

```text
API DTO -> view-model do cliente
mock temporario -> view-model do cliente
```

Nao colocar regra de negocio real aqui. Calculo de preco, estoque, limite,
status e assinatura pertence ao backend.
